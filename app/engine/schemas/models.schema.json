{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Model Definitions",
    "description": "Validates models.json AND <provider>-models.json (profile-level provider model files). Model ids are arbitrary keys; each entry must carry a 'name'. 'variants' is a map of named reasoning overlays: each value is a settings object whose keys follow the provider's reasoning format - reasoningEffort (opencode / openai), thinking.type + thinking.budgetTokens (claude), or thinkingConfig.thinkingBudget (gemini).",
    "type": "object",
    "properties": {
        "models": {
            "type": "object",
            "additionalProperties": {
                "type": "object",
                "properties": {
                    "name": { "type": "string" },
                    "apiModelId": {
                        "type": "string",
                        "description": "Optional exact model ID sent to the gateway. Defaults to this entry's key. Use when the gateway expects a different ID than the one shown in the app (for example OrcaRouter expects its own catalog name)."
                    },
                    "variants": {
                        "type": "object",
                        "description": "Named reasoning overlays (e.g. { low: { reasoningEffort: low } }). Supported settings keys include reasoningEffort, thinking.budgetTokens, thinkingConfig.thinkingBudget; additional keys are allowed and pass through unchanged."
                    },
                    "reasoning": { "type": "boolean" },
                    "temperature": { "type": "number" },
                    "limit": { "type": "number" }
                },
                "required": [ "name" ]
            }
        }
    },
    "required": [ "models" ],
    "additionalProperties": false
}
